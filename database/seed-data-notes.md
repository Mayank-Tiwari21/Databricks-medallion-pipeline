# Seed data notes

Source CSVs live in `data/`. They are synthetic (Faker); no real PII.

- `customers.csv` — 10,000 rows, generated by `src/data_generation/generate_sample_data.py`
- `orders.csv` — not generated yet
- `products.csv` — not generated yet
